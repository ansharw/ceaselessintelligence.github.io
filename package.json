{
  "name": "ceaselessintelligence",
  "private": true,
  "description": "Ceaseless Intelligence landing page — static site + Cloudflare Worker (src/index.js) for the lead form.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
